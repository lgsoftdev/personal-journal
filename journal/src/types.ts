export enum MenuOptions {
  Calendar = 'Calendar',
  Notes = 'Notes',
  TelAdds = 'Tel/Adds',
}

export type telAddsObj = {
  name: string;
  address: string;
  phone: string;
  email: string;
  website: string;
};
